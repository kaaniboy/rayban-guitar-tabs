When formatting guitar tab files, follow these rules:
- Use /// to denote the start of a new page (not needed for the first page)
- Put at most 20 lines per page
- Don't allow any line to exceed the length of the marker on the first line
